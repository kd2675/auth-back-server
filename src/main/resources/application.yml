server:
  forward-headers-strategy: framework

spring:
  config:
    import:
      - optional:file:.env[.properties]
      - optional:file:auth-back-server/.env[.properties]
  application:
    name: auth-back-server
  profiles:
    active: local
    group:
      local: local
      test: test
      dev: dev
      prod: prod
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Asia/Seoul
  jackson:
    time-zone: Asia/Seoul
  cloud:
    discovery:
      client:
        serviceUrl:
          defaultZone: http://localhost:8761/eureka/
  security:
    oauth2:
      client:
        registration:
          naver:
            client-id: ${NAVER_CLIENT_ID}
            client-secret: ${NAVER_CLIENT_SECRET}
            client-name: Naver
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope:
              - name
              - email
          kakao:
            client-id: ${KAKAO_CLIENT_ID:kakao-client-id}
            client-secret: ${KAKAO_CLIENT_SECRET:kakao-client-secret}
            client-name: Kakao
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope:
              - profile_nickname
              - profile_image
              - account_email
        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
app:
  jwt:
    secret: ${AUTH_JWT_SECRET}
#    access-token-expiration-ms: 3600000
#    refresh-token-expiration-ms: 1209600000
    access-token-expiration-ms: 600000 #10분
    refresh-token-expiration-ms: 3000000 #30분
  security:
    issuer: http://localhost:9000
  oauth2:
    default-provider: naver
    channel:
      zeroq-front-service:
        redirect-uri: http://localhost:3003/login
      zeroq-front-admin:
        redirect-uri: http://localhost:3002/login
      muse-front-service:
        redirect-uri: http://localhost:3001/login
      semo-front-service:
        redirect-uri: http://localhost:3000/login
    registered-client:
      zeroq-front-service:
        client-id: zeroq-front-service-web
        redirect-uri: http://localhost:3003/login
        post-logout-redirect-uri: http://localhost:3003
      zeroq-front-admin:
        client-id: zeroq-front-admin-web
        redirect-uri: http://localhost:3002/login
        post-logout-redirect-uri: http://localhost:3002
      muse-front-service:
        client-id: muse-front-service-web
        redirect-uri: http://localhost:3001/login
        post-logout-redirect-uri: http://localhost:3001
      semo-front-service:
        client-id: semo-front-service-web
        redirect-uri: http://localhost:3000/login
        post-logout-redirect-uri: http://localhost:3000
  signup:
    manager-secret: ${AUTH_MANAGER_SIGNUP_SECRET:1234}

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    hostname: localhost
